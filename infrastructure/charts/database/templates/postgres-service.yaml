apiVersion: v1
kind: Service
metadata:
  name: {{ include "database.fullname" . }}-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: {{ include "database.fullname" . }}-db
